"""Scheduled job hooks for future evaluation workflow automation.

This project does not currently run background jobs. Keep scheduled work here
when reminders, deadline checks, or automatic notification jobs are added.
"""


def register_scheduled_jobs(_scheduler):
    """Attach scheduled jobs to an APScheduler instance when one is introduced."""

    return None
